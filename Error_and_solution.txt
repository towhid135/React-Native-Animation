# If GestureHandler doesn't fire any events or onGestureEvent doesn't gets fire then set everything
inside <GestureHandlerRootView style={{flex:1}}> <GestureHandlerRootView/>.

# If we pass useSharedValue to a child component then the view of the child component should be
a Animated.View.
